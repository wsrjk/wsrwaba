
version: '3.8'
services:
  waba-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - WABA_TOKEN=${WABA_TOKEN}
      - WABA_PHONE_ID=${WABA_PHONE_ID}
    restart: unless-stopped
